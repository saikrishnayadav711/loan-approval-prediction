<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.10.4/gsap.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="/style/style.css">
</head>
<body class="bg-gray-100">

    <nav class="bg-white shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
          
                <div class="flex-shrink-0 flex items-center">
                    <img class="h-8 w-8" src="/images/loan.jpg" alt="Logo"> <h1 class="font-medium italic font-sans	cursor-pointer">LOAN APP</h1>
                </div>

                <div class="hidden md:flex space-x-4 items-center">
                    <a href="/" class="text-gray-900 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium cursor-pointer hover:text-blue-500">Home</a>
                    <a href="/loan" class="text-gray-900 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium cursor-pointer hover:text-blue-500">Predict</a>
                    <a href="#scrollButton1"   class="text-gray-900 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium cursor-pointer  hover:text-blue-500">Insights</a>
                    <a href="#" class="text-gray-900 hover:text-gray-700 px-3 py-2 rounded-md text-sm font-medium cursor-pointer hover:text-blue-500">Contact</a>
                </div>

                <!-- Mobile Menu Button -->
                <div class="md:hidden flex items-center">
                    <button id="mobile-menu-button" class="text-gray-900 hover:text-gray-700 focus:outline-none focus:text-gray-700">
                        <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="md:hidden hidden">
            <a href="/views/index.ejs" class="block text-gray-900 hover:bg-gray-200 px-3 py-2 rounded-md text-base font-medium">Home</a>
            <a href="#" class="block text-gray-900 hover:bg-gray-200 px-3 py-2 rounded-md text-base font-medium">Predict</a>
            <a href="#" class="block text-gray-900 hover:bg-gray-200 px-3 py-2 rounded-md text-base font-medium">Insights</a>
            <a href="#" class="block text-gray-900 hover:bg-gray-200 px-3 py-2 rounded-md text-base font-medium">Contact</a>
        </div>
    </nav>
    <div class="container mx-auto max-w-3xl bg-white p-8 rounded-lg shadow-lg">
        <h1 class="text-3xl font-bold mb-8 text-gray-800">Predict Your Loan</h1>

        <!-- Error Message -->
        <div id="error" class="error hidden mb-4"></div>

        <form id="loanForm" class="space-y-6" onsubmit="handleSubmit(event)">
            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-venus-mars mb-2 text-gray-600"></i>
                <label for="gender" class="text-gray-700 font-medium">Gender:</label>
                <select id="gender" name="gender" class="form-select mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    <option value="">Select Gender</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                </select>
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-user-ring mb-2 text-gray-600"></i>
                <label for="maritalStatus" class="text-gray-700 font-medium">Marital Status:</label>
                <select id="maritalStatus" name="maritalStatus" class="form-select mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    <option value="">Select Status</option>
                    <option value="Married">Married</option>
                    <option value="Single">Single</option>
                </select>
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-graduation-cap mb-2 text-gray-600"></i>
                <label for="education" class="text-gray-700 font-medium">Education:</label>
                <select id="education" name="education" class="form-select mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    <option value="">Select Education</option>
                    <option value="Graduate">Graduate</option>
                    <option value="Not Graduate">Not Graduate</option>
                </select>
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-wallet mb-2 text-gray-600"></i>
                <label for="applicantIncome" class="text-gray-700 font-medium">Applicant Income:</label>
                <input type="number" id="applicantIncome" name="applicantIncome" class="form-input mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" placeholder="Income in USD">
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-wallet mb-2 text-gray-600"></i>
                <label for="coapplicantIncome" class="text-gray-700 font-medium">Co-applicant Income:</label>
                <input type="number" id="coapplicantIncome" name="coapplicantIncome" class="form-input mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" placeholder="Income in USD">
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-dollar-sign mb-2 text-gray-600"></i>
                <label for="loanAmount" class="text-gray-700 font-medium">Loan Amount:</label>
                <input type="number" id="loanAmount" name="loanAmount" class="form-input mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500" placeholder="Loan Amount in USD">
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-credit-card mb-2 text-gray-600"></i>
                <label for="creditHistory" class="text-gray-700 font-medium">Credit History:</label>
                <select id="creditHistory" name="creditHistory" class="form-select mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    <option value="">Select Credit History</option>
                    <option value="1">Good</option>
                    <option value="0">Bad</option>
                </select>
            </div>

            <div class="flex flex-col border-b-2 border-gray-300 pb-2">
                <i class="fas fa-home mb-2 text-gray-600"></i>
                <label for="propertyArea" class="text-gray-700 font-medium">Property Area:</label>
                <select id="propertyArea" name="propertyArea" class="form-select mt-1 border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
                    <option value="">Select Property Area</option>
                    <option value="Urban">Urban</option>
                    <option value="Semi-Urban">Semi-Urban</option>
                    <option value="Rural">Rural</option>
                </select>
            </div>

            <button type="submit" class="bg-blue-500 text-white px-6 py-3 rounded-lg shadow-md hover:bg-blue-600 transition duration-300 ease-in-out">Predict Loan</button>
        </form>

        <div id="loading" class="loading flex justify-center items-center mt-4 text-3xl text-blue-500">
            <i class="fas fa-spinner fa-spin"></i>
        </div>

        <div id="result" class="mt-4 text-lg font-semibold"></div>
    </div>
    <footer class="bg-gray-800 text-white py-6">
        <div class="container mx-auto flex flex-col md:flex-row justify-between items-center">
            <!-- About Section -->
            <div class="mb-4 md:mb-0">
                <h2 class="text-xl font-bold mb-2">Loan Prediction</h2>
                <p class="text-gray-400">Automate loan approval with our machine learning model.</p>
            </div>

            <!-- Social Media Icons -->
            <div class="flex space-x-4">
                <a href="#" class="footer-icon">
                    <i class="fab fa-facebook-f"></i>
                </a>
                <a href="#" class="footer-icon">
                    <i class="fab fa-twitter"></i>
                </a>
                <a href="#" class="footer-icon">
                    <i class="fab fa-linkedin-in"></i>
                </a>
                <a href="#" class="footer-icon">
                    <i class="fab fa-instagram"></i>
                </a>
            </div>

            <!-- Contact Section -->
            <div class="mt-4 md:mt-0">
                <p class="text-gray-400">© 2024 Loan Prediction, Inc. All rights reserved.</p>
            </div>
        </div>
    </footer>
    <script>
        function handleSubmit(event) {
            event.preventDefault(); // Prevent form submission via URL

            // Hide previous error message
            const errorDiv = document.getElementById('error');
            errorDiv.classList.add('hidden');
            errorDiv.innerText = '';

            // Simple validation
            const fields = ['gender', 'maritalStatus', 'education', 'applicantIncome', 'coapplicantIncome', 'loanAmount', 'creditHistory', 'propertyArea'];
            let isValid = true;

            fields.forEach(field => {
                const element = document.getElementById(field);
                if (!element.value) {
                    isValid = false;
                    element.classList.add('border-red-500');
                } else {
                    element.classList.remove('border-red-500');
                }
            });

            if (!isValid) {
                errorDiv.classList.remove('hidden');
                errorDiv.innerText = 'Please fill out all required fields.';
                return; // Stop if validation fails
            }

            // Show loading indicator
            document.getElementById('loading').style.display = 'flex';

            // Simulate a delay for loading
            setTimeout(() => {
                document.getElementById('loading').style.display = 'none';

                // Simulate result
                const isApproved = Math.random() > 0.5; // Randomly approve or reject
                document.getElementById('result').innerText = isApproved ? 'Your loan will be approved!' : 'Your loan will not be approved.';
            }, 3000); // 3 seconds delay
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-element-bundle.min.js"></script>
      <script src="/js/script.js"></script>
  

</body>
</html>
